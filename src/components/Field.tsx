import { Controller, Control, FieldValues, Path } from 'react-hook-form';
import { HelperText, TextInput } from 'react-native-paper';
export function NumberField<T extends FieldValues>({ control, name, label, suffix }: { control: Control<T>; name: Path<T>; label: string; suffix?: string }) { return <Controller control={control} name={name} rules={{ required: name === 'weight' ? 'Weight is required' : false, min: { value: 0, message: 'Cannot be negative' } }} render={({ field: { onChange, value }, fieldState: { error } }) => <><TextInput label={label} value={value} onChangeText={onChange} keyboardType="decimal-pad" mode="outlined" error={!!error} right={suffix ? <TextInput.Affix text={suffix} /> : undefined} /><HelperText type="error" visible={!!error}>{error?.message}</HelperText></> } />; }
