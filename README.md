# Jewellery Rate Calculator

An Expo + TypeScript Android app for calculating gold and silver jewellery prices with live, cached market rates.

## Run

```bash
npm install
npm run android
```

Rates use the public Gold API and open ER API, then are cached locally for offline use. A rate refresh runs every 15 minutes while the app is open when enabled in Settings.
